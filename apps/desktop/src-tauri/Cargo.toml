[package]
name = "deepseek-harness-desktop"
version = "0.1.0"
description = "DeepSeek Harness native desktop window"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
url = "2"
